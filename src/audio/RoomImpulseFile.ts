export type RoomImpulseFile = {
  id: string;
  fileName: string;
  channelCount: number;
  durationSeconds: number;
  sampleRate: number;
  isProcessing: boolean;
  isEnabled: boolean;
  color: string;
};
