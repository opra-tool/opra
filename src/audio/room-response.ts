export type RoomResponse = {
  id: string;
  color: string;
  fileName: string;
  numberOfChannels: number;
  durationSeconds: number;
  sampleRate: number;
  isProcessing: boolean;
  isEnabled: boolean;
};
